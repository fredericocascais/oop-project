/**
 * The PEC (Pending Event Container) package contains classes for managing and scheduling events in a discrete event simulation.
 * It provides a PEC class that implements a queue for storing and retrieving events based on their scheduled time.
 * The Event class represents a simulation event and can be extended to define specific event types.
 */
package pec;